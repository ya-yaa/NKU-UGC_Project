

# UGC 复现

- model_type：gcn
- ratio：50
- add_adj_to_node_features：True

|              | 异配因子alpha | ratio | 实际压缩比例 | supernode | 平均准确率 | 平均时间 |
| ------------ | ------------- | ----- | ------------ | --------- | ---------- | -------- |
| **Cora**     | 0.19          | 50    | 48.63%       | 1391      | 0.8306     | 13.5029  |
| **Citeseer** | 0.26          | 30    | 40.06%       | 1994      | 0.7492     | 7.9755   |
|              |               | 50    | 60.99%       | 1298      | 0.7222     | 16.0400  |





# 数据分析

| Dataset  | Nodes     | Edges       | Avg degree | Max degree | Feature dim | feature sparsity | label shape   |
| -------- | --------- | ----------- | ---------- | ---------- | ----------- | ---------------- | ------------- |
| Cora     | 2,708     | 10,556      | 3.8981     | 168        | 1433        | 0.9873           | [2708]        |
| Citeseer | 3,327     | 9,104       | 2.7364     | 99         | 3703        | 0.9915           | [3327]        |
| Facebook | 4,039     | 88,234      | 21.8455    | 1043       | 1283        | 0.9936           | [4039, 193]   |
| Twitter  | 81,306    | 2,420,766   | 29.7735    | 3373       | 216839      | 0.9946           | [81306, 4065] |
| Tweibo   | 2,320,895 | 50,655,143  | 21.8257    | 5188       | 1657        | 0.9956           | [2320895,8]   |
| Reddit   | 232,965   | 114,615,892 | 491.9876   | 21657      | 602         | 0.0              | [232965]      |

社交网络数据集基本信息：[torch_geometric.datasets.attributed_graph_dataset — pytorch_geometric documentation](https://pytorch-geometric.readthedocs.io/en/latest/_modules/torch_geometric/datasets/attributed_graph_dataset.html#AttributedGraphDataset)

## 社交网络数据集

来自：[torch_geometric.datasets.AttributedGraphDataset — pytorch_geometric documentation](https://pytorch-geometric.readthedocs.io/en/latest/generated/torch_geometric.datasets.AttributedGraphDataset.html)

- **Facebook**
  - **无向**
  - **节点：**用户；**边：**好友关系
  - 将每个自我中心网络作为标签，从用户资料提取属性；
- **Twitter**
  - 有向
  - **节点：**用户；**边：**关注关系
  - 属性多提取自用户档案、推文关键词或标签；
- **TWeibo**
  - **有向**
  - **节点：**微博用户；**边：**关注关系
  - **属性：**提取自用户资料中 1657 个热门标签 / 关键词
  - **类别：**根据用户年龄划分（共 8 类）



# 1、采样

> 原始数据集为清洗过后，删除了无标签节点，删除多余重复边

- 采样 ： sampling.py -> 生成 twitter_sampled/raw；tweibo_sampled/raw

| Dataset             | Nodes   | Edges    | Avg degree（有向） | Max degree | Feature dim | feature sparsity | label shape   |
| ------------------- | ------- | -------- | ------------------ | ---------- | ----------- | ---------------- | ------------- |
| **Facebook**        | 2888    | 63373    | 21.9436            | 755        | 1283        | 0.9935           | [2888,193]    |
| **Twitter**         | 23370   | 1124521  | 48.1181            | 2890       | 216839      | 0.9923           | [23370, 4065] |
| **twitter_sampled** | 2000    | 163467   | 81.7335            | 1428       | 216839      | 0.9799           | [2000, 2285]  |
| **Tweibo**          | 2279766 | 45625265 | 20.0131            | 5124       | 1657        | 0.9956           | [2279766, 8]  |
| **tweibo_sampled**  | 10,000  | 559315   | 55.9315            | 1012       | 1657        | 0.9946           | [10000, 8]    |

# 2、计算 Alpha

- 计算 Alpha ： get_alpha*.py -> 得到子图真实的 Alpha（记录下来）

> `Labeled Edges` 的意思是：
>
> 一条边的两个端点节点 `u` 和 `v` 都在 `label_dict` 里有标签，这条边就被算作一条 `labeled edge`。
>
> 只有`Labeled Edges` 参与alpha计算

**求得Alpha：**

|                     | **V1：首标签** | **V2：集合交集** | **V3：位置对齐匹配** | V4：Jaccard（0.5） |
| ------------------- | -------------- | ---------------- | -------------------- | ------------------ |
| **Facebook**        | 0.3554         | 0.1180           | 0.3308               | 0.3041             |
| **twitter_sampled** | 0.8489         | 0.3722           | 0.8019               | 0.9197             |
| **tweibo_sampled**  | 0.7902         | 0.7902           | 0.7902               | 0.7902             |



# 3、预处理

- **预处理 ： data_process*.py -> 生成最终给 UGC 运行的 .pt 文件**

**类别数（只统计有标签节点）：**

|                     | V1   | V2   | V3   | V4   |
| ------------------- | ---- | ---- | ---- | ---- |
| **Facebook**        | 151  | 1    | 73   | 72   |
| **twitter_sampled** | 782  | 109  | 426  | 898  |
| **tweibo_sampled**  | 8    | 8    | 8    | 8    |



# 4、标定桶宽

- **标定桶宽 ： calibrate_ugc.py -> 填入上一步得到的 Alpha，得到最佳桶宽**

| ratio=50            | v1                 | v2                 | v3                 | v4                 |
| ------------------- | ------------------ | ------------------ | ------------------ | ------------------ |
| **Facebook**        | 0.012705           | 0.006846           | 0.012217           | 0.011240           |
| **twitter_sampled** | 0.499023（35.30%） | 0.499512（17.05%） | 0.499512（31.05%） | 0.498047（47.10%） |
| **tweibo_sampled**  | 0.006846           | 0.006846           | 0.006846           | 0.006846           |



# 5、UGC效果

- **正式实验 ： UGC.py**

```cmd
python f:\毕设\UGC-Universal-Graph-Coarsening\UGC.py `
  --dataset twitter_sampled `
  --dataset_not_in_torch_geometric True `
  --edge_index_path "f:\毕设\Social-Network\data\twitter_sampled\ugc_v4\edge_index.pt" `
  --node_feat_path  "f:\毕设\Social-Network\data\twitter_sampled\ugc_v4\node_feat.pt" `
  --label_path      "f:\毕设\Social-Network\data\twitter_sampled\ugc_v4\label.pt" `
  --feature_size 216839 `
  --num_classes 898 `
  --model_type gcn `
  --ratio 50 `
  --add_adj_to_node_features True `
  --alpha  0.9197`
  --epochs 500 `
  --bin_width 0.498047
```

| **Facebook** | V1      | V2      | V3      | V4      |
| ------------ | ------- | ------- | ------- | ------- |
| **ratio**    | 0.4972  | 0.5045  | 0.5035  | 0.4983  |
| **acc**      | 0.4853  | 1       | 0.7202  | 0.8307  |
| **time**     | 15.7945 | 10.3088 | 12.5274 | 12.6232 |

| twitter_sampled | V1       | V2      | V3      | V4       |
| --------------- | -------- | ------- | ------- | -------- |
| **ratio**       | 0.3555   | 0.1680  | 0.3100  | 0.4785   |
| **acc**         | 0.1750   | 0.9200  | 0.5800  | 0.1800   |
| **time**        | 133.4752 | 67.7006 | 84.2040 | 120.0967 |

| tweibo_sampled | V1      | V2     | V3   | V4   |
| -------------- | ------- | ------ | ---- | ---- |
| **ratio**      | 0.4974  | -      | -    | -    |
| **acc**        | 0.3980  | 0.4040 | -    | -    |
| **time**       | 51.2538 | -      | -    | -    |



# 1、系统

用户上传数据
   ↓
数据检查与预览
   ↓
用户选择参数
   ↓
计算 alpha
   ↓
数据预处理
   ↓
标定 bin width
   ↓
运行 UGC 粗化
   ↓
展示粗图与映射关系
   ↓
选择 GNN 模型做分类
   ↓
展示结果与对比



> **1. 入口层：用户打开系统**
> 用户先访问 Flask 应用入口 app.py。
>
> 这个文件是整个系统的总入口，负责三类事情：
>
> - 定义页面路由，也就是“哪个 URL 对应哪个页面”
> - 接收表单提交、上传文件、创建实验
> - 调用后端流程和读取结果，再把数据传给模板页面展示
>
> 你可以把它理解成“总调度台”。现在主要路由有：
>
> - /：上传页
> - /experiments：创建实验
> - /experiments/<run_id>/running：运行页
> - /experiments/<run_id>/result：结果页
> - /experiments/<run_id>/gnn：GNN 测试页
>
> 它还包含一些辅助函数，比如：
>
> - prepare_uploaded_dataset()：处理上传文件，生成一个临时数据集目录
> - build_graph_preview()：读取真实 coarse graph 文件
> - build_original_mapping()：生成原图联动视图数据
> - load_state() / save_state()：管理实验状态
>
> **2. 上传页：用户提供数据和参数**
> 上传页模板在 templates/upload.html。
>
> 这个页面负责收集两类输入：
>
> 1. 数据输入
>    - 内置数据集选择
>    - 边集文件
>    - 标签文件
>    - 特征文件
> 2. 流程参数
>    - 多标签策略 v1/v2/v3/v4
>    - 是否无向化
>    - target ratio
>    - jaccard threshold
>    - 默认 GNN 模型
>    - 训练轮数
>    - 标定轮数
>
> 页面样式主要由 static/css/style.css 控制。
>
> 这个页面提交后，会把表单发到 app.py 的 create_experiment()。
>
> **3. 创建实验：把一次运行变成一个实验目录**
> 在 app.py 的 create_experiment() 里，会做几件事：
>
> 1. 生成一个 run_id
>    也就是本次实验的唯一编号。
> 2. 判断用户是用内置数据，还是上传了自定义数据
>    如果上传了文件，就调用 prepare_uploaded_dataset()，把文件存进：
>    uploads
>
> 结构会整理成类似：
>
> - uploads/<dataset_name>/raw/edgelist.txt
> - uploads/<dataset_name>/raw/labels.txt
> - uploads/<dataset_name>/raw/attrs.npz
>
> 1. 把本次实验的参数整理成状态对象
>    然后写进：
>
> - outputs/<run_id>/state.json
>
> 这一层的目标是：把“用户这次点击运行”的上下文保存下来，后面运行页、结果页、测试页都围绕这次实验展开。
>
> **4. 运行页：统一流程开始执行**
> 运行页模板在 templates/running.html。
>
> 这个页面的作用不是自己做计算，而是展示：
>
> - 当前阶段
> - 日志
> - 中间结果
> - 最终是否完成
>
> 实际运行逻辑仍然在 app.py 的 running_page() 中触发。
>
> 当用户第一次进入运行页时，running_page() 会调用：
>
> - algorithms/pipeline.py
>
> 也就是整个后端统一流程控制器。
>
> **5. 总流程控制：pipeline 是整个系统主链**
> algorithms/pipeline.py 是核心文件之一。
>
> 它的职责是把你原来分散的脚本组织成一个标准流程，按顺序执行：
>
> 1. 计算 alpha
> 2. 数据预处理
> 3. 标定 bin width
> 4. 运行 UGC
> 5. 提取 coarse graph 映射
> 6. 把结果统一写入输出目录
>
> 这个文件里的核心类是 UGCPipeline，核心方法是 run()。
>
> 它还支持：
>
> - data_root：可以跑内置数据，也可以跑上传数据
> - strategy：支持 v1-v4
> - force_undirected
> - stage_callback
>   也就是每跑完一个阶段，就把状态回写给运行页
>
> 所以运行页能显示“当前阶段”和中间结果，就是因为 pipeline 在执行时不断回调状态。
>
> **6. Alpha 计算层**
> algorithms/alpha_service.py
>
> 这个文件专门负责 alpha 的计算封装。
> 它的作用是根据用户选的策略，把请求分发到不同脚本：
>
> - v1 -> scripts/get_alpha/get_alpha1.py
> - v2 -> scripts/get_alpha/get_alpha2.py
> - v3 -> scripts/get_alpha/get_alpha3.py
> - v4 -> scripts/get_alpha/get_alpha4.py
>
> 也就是说，这个文件不重新发明 alpha 算法，而是把旧脚本统一包装成结构化返回值：
>
> - alpha 值
> - labeled edges
> - total edges
> - coverage
> - strategy
>
> 它是“旧脚本到新系统”的第一层封装。
>
> **7. 预处理层**
> algorithms/preprocess_service.py
>
> 这个文件负责把原始数据转成 UGC 主程序能直接吃的格式。
>
> 它同样按策略分发到旧脚本：
>
> - v1 -> scripts/data_process/data_process1.py
> - v2 -> scripts/data_process/data_process2.py
> - v3 -> scripts/data_process/data_process3.py
> - v4 -> scripts/data_process/data_process4.py
>
> 它做完后会拿到并返回这些核心产物：
>
> - edge_index.pt
> - node_feat.pt
> - label.pt
>
> 还会顺手统计：
>
> - 节点数
> - 边数
> - 特征维度
> - 类别数
> - 特征是否为 dense
>
> 这些产物会存到：
>
> - data/<dataset>/ugc_v1
> - data/<dataset>/ugc_v2
> - data/<dataset>/ugc_v3
> - data/<dataset>/ugc_v4
>
> 或者上传数据对应的 uploads/... 下。
>
> **8. Bin Width 标定层**
> algorithms/calibrate_service.py
>
> 这个文件负责“找合适的 bin width”。
>
> 它做的事是：
>
> - 根据 alpha、特征维度、类别数、目标压缩率等参数
> - 反复调用原始 UGC.py
> - 每次传一个不同的 --bin_width
> - 从标准输出里提取：
>   Graph reduced by: xx percent
> - 用二分法不断逼近目标压缩率
>
> 所以它不是直接计算公式，而是“试出来”。
>
> 它最后返回：
>
> - 推荐 bin_width
> - 最后一轮实际压缩率
> - 每一轮尝试记录
> - 标定时的 stdout/stderr
>
> 运行页看到的 bin width 和压缩率中间结果，主要就是这里来的。
>
> **9. 真正运行 UGC 的层**
> algorithms/ugc_service.py
>
> 这个文件负责真正启动原始 UGC 主程序：
> UGC-Universal-Graph-Coarsening/UGC.py
>
> 它会把这些信息拼成命令：
>
> - 数据路径
> - feature_size
> - num_classes
> - alpha
> - bin_width
> - model_type
> - ratio
> - epochs
>
> 然后运行 UGC，并从输出中提取：
>
> - 粗化率
> - supernode 数量
> - 原始节点数
> - 平均准确率
> - 平均耗时
>
> 所以：
>
> - 标定阶段也会用它的同类逻辑
> - GNN 测试页也会再次用它
>
> 这个文件本质是“UGC 命令执行器 + 输出解析器”。
>
> **10. 真实 coarse graph 提取层**
> algorithms/coarsening_service.py
>
> 这是后面为了可视化新加的重要文件。
>
> 它的职责是：
>
> - 按 UGC.py 的真实逻辑重新计算 partition
> - 生成真实 supernode 映射
> - 生成真实 coarse adjacency
> - 保存成前端可用的 JSON
>
> 也就是说，它不是伪造图，而是重建：
>
> - 哪些原始节点属于哪个 supernode
> - coarse graph 的真实边权
> - 每个 supernode 的成员列表
> - 每个 supernode 的标签分布
> - 邻接 supernode
> - coarse label
> - feature norm
>
> 它最终会写出：
>
> - outputs/<run_id>/coarse_graph.json
>
> 结果页和联动视图现在依赖的就是这份文件。
>
> **11. 结果落盘**
> 当 pipeline 跑完后，会把结果保存到：
>
> - outputs/<run_id>/summary.json
> - outputs/<run_id>/ugc_stdout.log
> - outputs/<run_id>/ugc_stderr.log
> - outputs/<run_id>/coarse_graph.json
>
> 其中：
> summary.json 是整次实验的结构化摘要
> coarse_graph.json 是可视化核心数据
>
> **12. 结果页：粗图、supernode、联动**
> 结果页模板在 templates/result.html
>
> 这个页面展示四类信息：
>
> 1. 指标概览
>
>    - alpha
>    - bin width
>    - 压缩率
>    - 平均准确率
>    - supernode 数
>    - 类别数
>
> 2. 分类结果图
>    通过 static/js/result_chart.js 用 ECharts 画雷达图。
>
> 3. coarse graph
>
>    通过
>
>     
>
>     
>
>    用 Cytoscape.js 画真实 coarse graph。
>
>    这里现在是：
>
>    - 展示全部 supernode
>    - 只展示高权重边子集
>    - 点击 supernode 可看详情
>
> 4. 原图映射联动视图
>
>    同样由
>
>     
>
>     
>
>    驱动。
>
>    点击 coarse supernode 后：
>
>    - 下方显示它对应的原始节点子图
>    - 红色节点是成员节点
>    - 蓝灰节点是上下文邻居
>
> 现在还支持反向联动：
>
> - 点击原始节点
> - 反向高亮上方所属 supernode
>
> **13. 前端图交互逻辑**
> static/js/graph_view.js
>
> 这是结果页最关键的前端逻辑文件。
>
> 它负责：
>
> - 初始化 coarse graph Cytoscape
> - 初始化原图联动 Cytoscape
> - 点击 supernode 时更新详情面板
> - 点击 supernode 时渲染原图子图
> - 点击原始节点时反向高亮 coarse supernode
>
> 你可以把它看成“结果页图交互控制器”。
>
> static/js/result_chart.js
>
> 这个文件比较单纯，负责：
>
> - 初始化 ECharts
> - 把压缩率、准确率、alpha、bin width 画成结果图
>
> **14. GNN 测试页**
> 模板在 templates/gnn_test.html
>
> 这个页面的作用是：
>
> - 不重新做 alpha、预处理、标定
> - 直接复用当前实验已经得到的 coarse 参数
> - 让用户改 GNN 模型和训练轮数
> - 再次运行 UGC 的训练测试部分
>
> 后端仍由 app.py 的 gnn_test_page() 处理。
> 它会读取当前实验的：
>
> - alpha
> - bin_width
> - edge_index.pt
> - node_feat.pt
> - label.pt
>
> 然后再次调用 algorithms/ugc_service.py 去测不同模型。
>
> 测试结果会存到：
>
> - outputs/<run_id>/gnn_tests/*.json
>
> **15. 公共工具层**
> algorithms/utils.py
>
> 这个文件负责一些公共基础能力，比如：
>
> - 创建目录
> - 动态加载旧脚本
> - 运行子进程
> - 写 JSON / 读 JSON
> - 生成时间戳
> - 解析正则结果
> - 解析 Python 命令
> - slugify() 处理上传数据名
>
> 它不是业务逻辑，但很多 service 都依赖它。
>
> **16. 配置层**
> config.py
>
> 这个文件统一管理目录路径：
>
> - DATA_DIR
> - UPLOADS_DIR
> - OUTPUTS_DIR
> - SCRIPTS_DIR
> - SOURCE_UGC_DIR
>
> 作用是避免每个文件都手写一遍路径。
>
> **17. 模型占位层**
> 这几个文件目前还是轻量占位，为以后接 MySQL 做准备：
>
> - models/dataset.py
> - models/experiment.py
> - models/result.py
>
> 它们现在更多是“概念模型”：
>
> - 数据集
> - 实验
> - 结果
>
> 后面接数据库时，可以把这里扩成 ORM 模型。
>
> **18. 页面外观层**
> templates/base.html
> 是所有页面共用的基础模板，统一引入：
>
> - CSS
> - Cytoscape.js
> - ECharts
> - JS 文件
>
> static/css/style.css
> 负责整个系统的界面样式：
>
> - 顶部导航
> - 面板
> - 表单
> - 指标卡片
> - 图画布
> - 日志区
> - 响应式布局
>
> **19. 旧脚本层**
> scripts
> 这里是你原来的算法脚本保留区。
>
> 它们目前不直接给页面调用，而是通过 service 层被封装进新系统。
>
> 主要包括：
>
> - get_alpha*.py
> - data_process*.py
> - calibrate_ugc.py
> - UGC.py
>
> 这里的设计思路是：
> “旧算法逻辑尽量不乱动，新系统在外面包一层统一接口”。



# 创新工作总结

## 1. 当前研究背景

我的毕设主题是基于图粗化的社交网络分析系统，当前采用的核心图粗化方法为 **UGC（Universal Graph Coarsening）**。  
原始 UGC 的基本流程是：

1. 利用特征和邻接信息构造增强特征；
2. 通过哈希方法将原始节点分配到 super-node；
3. 根据 super-node 划分结果构造粗化图；
4. 用粗化图进行下游节点分类等任务。

目前我的工作重点是：  
**在不改下游 GNN 的前提下，仅在 UGC 方法本身上做轻量创新。**

---

## 2. 已增加的统计指标

为了分析 UGC 在单标签数据集上的粗化质量，我首先增加了两个标签纯度相关指标：

- 平均标签纯度（AvgPurity）
- 低纯度超节点占比（LowPurityRatio）

这两个指标的目的，是衡量 **super-node 内部标签是否一致**。

---

## 3. 平均标签纯度（AvgPurity）

### 3.1 单个 super-node 的标签纯度定义

设粗化后得到的超节点集合为：

\[
\mathcal{S}=\{S_1,S_2,\dots,S_m\}
\]

其中 \(S_k\) 表示第 \(k\) 个 super-node，内部包含若干原始节点。

对每个超节点 \(S_k\)，其标签纯度定义为：

\[
\mathrm{Purity}(S_k)
=
\frac{\max_c |\{u \in S_k : y_u = c\}|}{|S_k|}
\]

其中：

- \(y_u\) 表示原始节点 \(u\) 的单标签类别；
- \(|S_k|\) 表示超节点 \(S_k\) 中原始节点的数量；
- \(\max_c |\{u \in S_k : y_u = c\}|\) 表示该超节点中出现次数最多的类别对应的节点数量。

### 3.2 含义解释

这个指标表示：

> 一个 super-node 中，数量最多的那个类别，占整个 super-node 的比例有多大。

- 如果 purity 接近 1，说明这个 super-node 内部标签非常一致；
- 如果 purity 较低，说明这个 super-node 内部混入了较多不同类别的节点。

---

## 4. 平均标签纯度（AvgPurity）定义

在所有 super-node 上取平均，定义：

\[
\mathrm{AvgPurity}
=
\frac{1}{m}\sum_{k=1}^{m}\mathrm{Purity}(S_k)
\]

其中：

- \(m\) 表示粗化后 super-node 的总数。

### 4.1 含义解释

AvgPurity 用来衡量：

> 整个粗化结果中，super-node 的整体标签一致性水平。

- AvgPurity 越高，说明整体上 coarse partition 越“纯”；
- AvgPurity 越低，说明粗化后大量 super-node 存在标签混杂问题。

---

## 5. 低纯度超节点占比（LowPurityRatio）

给定一个纯度阈值 \(\tau\)，定义低纯度超节点占比为：

\[
\mathrm{LowPurityRatio}_{\tau}
=
\frac{|\{S_k : \mathrm{Purity}(S_k) < \tau\}|}{m}
\]

其中：

- \(\tau\) 为低纯度判定阈值；
- 当前实验中默认取 \(\tau = 0.7\)。

### 5.1 含义解释

这个指标表示：

> 在所有 super-node 中，有多少比例的 super-node 纯度低于预设阈值。

- 如果 LowPurityRatio 较高，说明粗化结果中“混杂组”较多；
- 如果 LowPurityRatio 较低，说明粗化结果中大部分 super-node 已经比较纯。

### 5.2 当前使用方式

目前我使用：

- `AvgPurity`：衡量整体粗化标签一致性；
- `LowPurityRatio@0.7`：衡量低质量 super-node 的比例。

这两个指标主要用于：

1. 分析原始 UGC 在单标签图上的表现；
2. 验证后续优化是否真的改善了 super-node 质量。

---

## 6. 第一阶段优化：低纯度 super-node 局部二分拆分

在增加 purity 指标后，我尝试了一种后处理优化方法：

> 对低纯度 super-node 做一次局部二分拆分。

### 6.1 设计动机

原始 UGC 得到 super-node 后，默认直接进行：

- coarse feature 聚合；
- coarse label 聚合；
- coarse adjacency 构造。

但如果某些 super-node 的 purity 很低，说明它们内部标签混杂严重。  
此时继续直接使用这些 super-node，可能会带来：

- coarse label 不可靠；
- coarse feature 被不同类别节点平均后变糊；
- 下游分类受噪声影响。

因此，我加入了一个局部细化步骤：

> 对低纯度 super-node，不直接保留，而是在其内部再做一次轻量二分。

---

### 6.2 bad super-node 的定义

设 purity 阈值为 \(\tau\)，如果某个 super-node 满足：

\[
\mathrm{Purity}(S_k) < \tau
\]

则认为它是一个低纯度 super-node。

在实际处理中，还增加了一个最小节点数限制，避免对过小的 super-node 进行拆分。

---

### 6.3 采用的拆分方法

我采用的是：

## **KMeans 局部二分（\(k=2\)）**

具体做法是：

对每个低纯度 super-node \(S_k\)：

1. 取出其中所有原始节点的特征 \(\{x_u \mid u \in S_k\}\)；
2. 在这个局部节点集合上执行一次 `KMeans(n_clusters=2)`；
3. 将原 super-node \(S_k\) 拆分为两个子 super-node：

\[
S_k \rightarrow S_k^{(1)} \cup S_k^{(2)}
\]

于是原来一个混杂 super-node 被替换为两个更小的 super-node。

### 6.4 方法含义

这个步骤的本质是：

> 用无监督特征聚类的方式，把内部混杂的 coarse node 局部拆开。

这里使用 KMeans 的原因是：

- 实现简单；
- 不改变 UGC 主干；
- 只在低纯度 super-node 内部做局部操作；
- 不涉及全图重聚类。

---

### 6.5 拆分后的后续处理

当低纯度 super-node 被拆分后，原来的 super-node 划分已经变化，因此必须重新构造 coarse graph。

需要重新构造的对象包括：

#### 1）重新构造 \(P_{\text{hat}}\)

如果节点 \(v_i\) 属于 super-node \(S_j\)，则：

\[
(P_{\text{hat}})_{ij}=1
\]

否则为 0。

#### 2）重新构造 \(P\)

\[
P
=
P_{\text{hat}}
\cdot
\mathrm{diag}\left(
|S_1|^{-1/2},|S_2|^{-1/2},\dots,|S_m|^{-1/2}
\right)
\]

#### 3）重新构造 coarse feature

保持原始 UGC 的平均式聚合方式：

\[
X_c = P^\top X
\]

#### 4）重新构造 coarse label

保持原始 UGC 的投票式聚合逻辑：

\[
Y_c = \arg\max(P^\top Y)
\]

#### 5）重新构造 coarse adjacency

\[
A_c = P_{\text{hat}}^\top A P_{\text{hat}}
\]

### 6.6 小结

这一阶段的创新点可以概括为：

> 在原始 UGC 哈希分组之后，增加一个基于标签纯度的局部二分拆分模块，以减少低纯度 super-node 带来的混杂问题。

---

## 7. 第二阶段优化方向：超节点特征聚合方式改进

在后续实验中发现：

- 某些数据集在高压缩率下，即使 super-node 的 purity 已经不低，分类效果仍明显下降；
- 这说明问题可能不只是标签纯度，而更可能是 coarse feature 的表达能力不足。

因此，我进一步考虑：

> 不改 UGC 分组，不改下游 GNN，只改 super-node 的特征聚合方式。

---

## 8. 相似度加权聚合方案

这一方案的核心思想是：

> 在一个 super-node 内部，不再让所有原始节点等权参与平均，而是让更接近组原型的节点获得更大的聚合权重。

---

### 8.1 原始均值原型

设 super-node 为 \(S_k\)，其组内均值原型定义为：

\[
\mu_k=\frac{1}{|S_k|}\sum_{u\in S_k}x_u
\]

其中：

- \(x_u\) 是原始节点 \(u\) 的特征向量；
- \(\mu_k\) 表示该 super-node 的初始均值原型。

---

### 8.2 节点与组原型的相似度

对组内每个节点 \(u \in S_k\)，计算其与组原型 \(\mu_k\) 的相似度：

\[
z_u = \mathrm{sim}(x_u,\mu_k)
\]

这里采用余弦相似度：

\[
\mathrm{sim}(x_u,\mu_k)
=
\frac{x_u^\top \mu_k}{\|x_u\|_2 \, \|\mu_k\|_2}
\]

### 8.3 含义解释

这个相似度表示：

> 节点 \(u\) 的特征方向，与该 super-node 的整体原型方向有多接近。

- 相似度高：说明该节点更像这个组的“代表”；
- 相似度低：说明该节点更像边界点或离群点。

---

### 8.4 将相似度转换为权重

为了让相似度参与聚合，需要将其转换成权重：

\[
w_u
=
\frac{\exp(\gamma z_u)}
{\sum_{v\in S_k}\exp(\gamma z_v)}
\]

其中：

- \(w_u \ge 0\)
- \(\sum_{u\in S_k} w_u = 1\)
- \(\gamma > 0\) 为温度系数

### 8.5 温度系数 \(\gamma\) 的作用

\(\gamma\) 用于控制权重分布的尖锐程度：

- \(\gamma\) 较小：各节点权重差异小，更接近普通均值；
- \(\gamma\) 较大：更接近组中心的节点权重明显更大，离群节点权重更小。

当前建议实验范围为：

\[
\gamma \in \{5, 10, 20\}
\]

---

### 8.6 相似度加权聚合公式

最终，新的 super-node 特征定义为：

\[
x_{S_k}^{new}
=
\sum_{u\in S_k} w_u x_u
\]

### 8.7 含义解释

这个公式表示：

> 组内所有节点仍然都参与聚合，但不再是等权平均，而是按“谁更像组原型”来决定贡献大小。

因此：

- 更像组中心的节点，对 coarse feature 贡献更大；
- 噪声节点、边界节点对 coarse feature 的影响被减弱。

---

## 9. 相似度加权聚合方案的完整流程

该方案的流程如下：

### Step 1：保持原始 UGC 分组不变
仍然使用原始 UGC 哈希分组结果得到 super-node 划分。

### Step 2：对每个 super-node 计算均值原型
\[
\mu_k=\frac{1}{|S_k|}\sum_{u\in S_k}x_u
\]

### Step 3：计算组内每个节点与原型的余弦相似度
\[
z_u=\mathrm{sim}(x_u,\mu_k)
\]

### Step 4：通过 softmax 得到节点权重
\[
w_u
=
\frac{\exp(\gamma z_u)}
{\sum_{v\in S_k}\exp(\gamma z_v)}
\]

### Step 5：使用加权聚合生成新的 super-node 特征
\[
x_{S_k}^{new}
=
\sum_{u\in S_k}w_u x_u
\]

### Step 6：保持其他部分不变
该方案中：

- 不改变原始哈希分组结果；
- 不改变 \(P_{\text{hat}}\) 和 \(P\) 的定义方式；
- 不改变 coarse adjacency 的构造；
- 不改变 coarse label 的聚合方式；
- 只替换 coarse feature 的生成逻辑。

---

## 10. 当前创新工作的整体脉络

到目前为止，我围绕 UGC 在单标签数据集上的优化，主要完成了以下三步：

### 10.1 第一步：增加粗化质量分析指标
新增：

- 平均标签纯度 AvgPurity
- 低纯度超节点占比 LowPurityRatio

用于衡量粗化后 super-node 的标签一致性。

---

### 10.2 第二步：尝试低纯度 super-node 的局部二分拆分
对低纯度 super-node：

- 利用 KMeans（\(k=2\)）在其内部做局部二分；
- 然后根据新 groups 重建 coarse graph。

这一方向的核心目标是：

> 通过减少混杂 super-node，提高 coarse graph 的标签一致性。

---

### 10.3 第三步：尝试相似度加权特征聚合
不改变 UGC 的分组和图结构，只修改 coarse feature 的聚合方式：

- 先计算组均值原型；
- 再计算节点与原型的相似度；
- 将相似度转化为 softmax 权重；
- 最后做加权聚合。

这一方向的核心目标是：

> 提高 coarse feature 的表达能力，减少均值聚合对关键判别特征的稀释。

---

## 11. 当前阶段的理解

当前实验现象表明：

1. **标签纯度是一个重要分析指标，但不一定是唯一瓶颈**；
2. 在高压缩率下，即使 purity 已经不低，分类性能仍可能下降；
3. 因此，除了 purity 相关的后处理外，**超节点特征聚合方式**很可能是更关键的优化方向。

---

## 12. 当前工作总结（一句话版）

目前的创新工作可以概括为：

> 在原始 UGC 方法基础上，先引入 super-node 标签纯度指标用于分析粗化质量；随后尝试基于 purity 的局部二分拆分策略对低纯度 super-node 进行后处理；进一步提出基于节点与组原型相似度的加权特征聚合方案，以增强 coarse feature 的表达能力，而不改变 UGC 主干和下游 GNN 结构。







> 相似度加权聚合方案请在现有 UGC 方法中实现一种“相似度加权聚合”的超节点特征生成方式，用来替代原始的均值聚合。要求是不改变原始 UGC 的哈希分组方式，也不改变 coarse adjacency 和 coarse label 的生成逻辑，只修改 coarse feature（super-node feature）的聚合方式。
>
> 设粗化后某个 super-node 为 \(S_k\)，其包含的原始节点特征为 \(\{x_u \mid u \in S_k\}\)。
>
> 第一步，先计算该 super-node 的均值原型特征：
>
> \[
> \mu_k = \frac{1}{|S_k|}\sum_{u \in S_k} x_u
> \]
>
> 第二步，计算每个节点特征与该 super-node 原型之间的相似度。这里使用余弦相似度：
>
> \[
> z_u = \mathrm{sim}(x_u, \mu_k)
> \]
>
> 其中：
>
> \[
> \mathrm{sim}(x_u, \mu_k)
> =
> \frac{x_u^\top \mu_k}{\|x_u\|_2 \, \|\mu_k\|_2}
> \]
>
> 第三步，将相似度通过 softmax 转换成节点权重：
>
> \[
> w_u
> =
> \frac{\exp(\gamma z_u)}
> {\sum_{v \in S_k}\exp(\gamma z_v)}
> \]
>
> 其中：
> \- \(w_u \ge 0\)
> \- \(\sum_{u \in S_k} w_u = 1\)
> \- \(\gamma > 0\) 为温度系数，用于控制权重分布的尖锐程度
>
> 第四步，使用这些权重对组内节点特征进行加权聚合，得到新的 super-node 特征：
>
> \[
> x_{S_k}^{new}
> =
> \sum_{u \in S_k} w_u x_u
> \]
>
> 请将上述方法集成到现有 UGC 代码中，替换原本的均值式 coarse feature 聚合方式。
